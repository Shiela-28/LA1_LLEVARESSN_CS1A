//Task3: Numbers

// Declare variables to store two favorite numbers.
let firstFavNum = 8;
let secondFavNum = 5;

let additionResult = firstFavNum + secondFavNum; // Calculate the sum of the two numbers.
let subtractionResult = firstFavNum - secondFavNum; // Calculate the difference between the two numbers.
let multiplicationResult = firstFavNum * secondFavNum; // Calculate the product of the two numbers.
let divisionResult = firstFavNum / secondFavNum; // Calculate the product of the two numbers.
let modulusResult = firstFavNum % secondFavNum; // Calculate the remainder when the first number is divided by the second number.
let exponentiationResult = firstFavNum ** secondFavNum; // Calculate the first number raised to the power of the second number.

// Display the original numbers.
console.log("First Favorite Number:", firstFavNum);
console.log("Second Favorite Number:", secondFavNum);

// Display the results of the arithmetic operations.
console.log("Addition:", additionResult);
console.log("Subtraction:", subtractionResult);
console.log("Multiplication:", multiplicationResult);
console.log("Division:", divisionResult);
console.log("Modulus:", modulusResult);
console.log("Exponentiation:", exponentiationResult);